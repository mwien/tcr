/* bipartite graph check
 */

//START
// traverse through graph with bfs
// assign labels 0 and 1
// if child is unexplored it gets different label from parent and put in the queue
// if already visited check if labels are different
//END
